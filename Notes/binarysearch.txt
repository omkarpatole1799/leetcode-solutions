- decide start and end points
- decide middle point of array Math.floor((start + end) / 2)

- now check following
if target value < the array[midpoint] then switch midpoint index
                - if target value is left to the midpoint then switch end ( end = end -1)
                - if target value is right to the midpoint then switch start ( start = start +1)
if target value > the array[midpoint] then also switch midpoint index
                - if target value is left to the midpoint then switch end ( end = end -1)
                - if target value is right to the midpoint then switch start ( start = start +1)
if the target value === array[midpoint]

if no element found return -1

